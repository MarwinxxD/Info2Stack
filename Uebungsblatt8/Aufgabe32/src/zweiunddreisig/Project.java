package zweiunddreisig;

/*public class Project {
    public void linkTask(Task task) throws IllegalArgumentException {
        if (checkTask(task)) {
            tasks.add(task);
        } else {
            throw new IllegalArgumentException("task is due after Project");
        }
    }

    public boolean checkTask(Task task) {
        if (task.dueDate().isBefore(this.dueDate())) {
            return true;
        } else {
            return false;
        }
    }
}*/

public class Project {

}
